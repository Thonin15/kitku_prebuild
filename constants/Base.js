import { Dimensions } from 'react-native';

export const { height: _SCREEN_HEIGHT, width: _SCREEN_WIDTH } =
	Dimensions.get('screen');
export const _BUTTON_HEIGHT = 60;
export const _RADIOS = 3;
